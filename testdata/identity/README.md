# Identity fixtures

Fixtures cover npm scoped names, mixed case, punctuation, invalid path separators/Unicode, and typosquat-like but syntactically valid names. Go tests load all JSON fixture files deterministically.

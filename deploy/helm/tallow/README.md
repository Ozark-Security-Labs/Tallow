# Tallow Helm chart (experimental)

This chart is an experimental planning artifact. Docker Compose remains the primary local deployment path until Kubernetes deployment is validated.

Required services: PostgreSQL, NATS JetStream, filesystem/PVC storage, and the Tallow API container. Optional LLM providers and community signal sharing are disabled by default.

# ADR 0004: Deterministic First, LLM Enrichment Optional

Tallow uses deterministic analyzers as the source of truth. LLMs may produce narrative summaries over deterministic evidence, but cannot set canonical severity.

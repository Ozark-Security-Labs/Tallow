# Security

See `docs/security/threat-model.md`, `safe-unpack.md`, `auth.md`, and `llm-usage.md`.

## Foundation threat model summary

Foundation explicitly documents hostile package intake, archive extraction risks, future auth/notification boundaries, prompt-injection risks, and the rule that deterministic evidence/scoring owns severity.
